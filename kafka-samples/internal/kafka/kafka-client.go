
import (
	"fmt"
)

type kafka-client struct {
	bootstrap-servers string
	avro-enabled bool
	schemaFile string
}


func (kc *kafka-client) publish(msg interface{}) {
	
}

func (kd *kafka-client) consume(msg interface{}) {

}